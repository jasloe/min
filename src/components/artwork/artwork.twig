<div class="artwork__wrapper">
	<div class="artwork-image__inner">
		<div class="artwork-image">

			{{ images }}
		</div>
	</div>
	<div class="artwork-metadata">
		<h2 class="field artwork-metadata__title">{{ title }}</h2>
		<div class="field artwork-metadata__dates">{{ dates }}</div>
		<div class="field artwork-metadata__materials">{{ materials }}</div>
		<div class="field artwork-metadata__dimensions">{{- dimensions -}}</div>
	</div>
</div>
